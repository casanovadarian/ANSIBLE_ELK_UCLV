
output {
  elasticsearch {
    hosts => [
      "https://10.12.1.34:9200",
      "https://10.12.1.35:9200",
      "https://10.12.1.36:9200"
    ]

    index => "%{index_name}-%{indice_local}"

    user => "elastic"
    password => "h9oveUPLR32TPdnB"

    ssl_enabled => true
    ssl_certificate_authorities => ["/etc/logstash/certs/elasticsearch-ca.pem"]
  }

  # stdout { codec => rubydebug }
}
